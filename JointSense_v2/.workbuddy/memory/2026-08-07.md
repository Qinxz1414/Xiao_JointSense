# 2026-08-07

## ELISA pg/mL estimation from clipboard photos
- User pointed out ELISA results should be pg/mL values, not just color levels. Re-read Rule/SKILL.md: it only defines well-interior color palette (Blank→Very strong), NO standard curve / OD→pg/mL mapping.
- 样本.pptx contains the standard concentration ladder: TNF/IL-1β = 0,2,5,10,20,50,100,200,500 pg/mL; IL-6 = 0,5,10,20,50,100,200,500,1000 pg/mL.
- Attempted to build a color→concentration calibration from 样本.pptx standard-curve photos, but they are full ELISA-plate phone photos (3072×4096) with NO documented well→concentration layout; naive sampling gives a non-monotonic, unusable relationship. So photo-color→pg/mL is not reliable from this file.
- Key fact: real pg/mL in ELISA comes from a microplate reader OD + 4PL standard-curve fit, not from photo color.
- User accepted a coarse ordinal proxy: map qualitative levels (Blank/Very weak/Weak/Moderate/Strong/Very strong, by tealness B−R thresholds) to nearest ladder concentration. Mapping: TNF/IL-1β {0,2,5,20,100,500}; IL-6 {0,5,10,50,200,1000}.
- Result saved to Rule/_pgmL_estimate.md. Summary: Photo1 all 0; Photo2 TNF=0/IL-6=0/IL-1β≈2; Photo3 TNF≈100/IL-6≈10/IL-1β≈500 (median pg/mL).
- Cleaned up temp scripts, _stdcurve dir, _calibration.npy.

## Embed quantified data into Android app (JointSense_v2)
- Created `app/src/main/java/cloud/univ/jointsense/data/BuiltInData.kt`: object holding 3 `TestSession`s (one per photo) with 3 `TestResult`s each (TNF-α/IL-6/IL-1β) using median pg/mL: P1 all 0; P2 IL-1β≈2; P3 TNF≈100/IL-6≈10/IL-1β≈500. Fixed capture timestamps (GMT+8 2026-08-06 19:25/19:31/23:20). RGB stats flagged as representative visual estimates (concentration is authoritative).
- `JointSenseViewModel.init` now seeds `BuiltInData.sessions` into `TestRepository` when `loadSessions()` is empty (first launch / after clear-all), so the dashboard isn't empty. User data always wins.
- `OaIndex.caps` raised from placeholder 6/8/4 to realistic working ranges 500/1000/500 pg/mL (uppermost points of the 样本.pptx ladder) so the AI index no longer saturates at grade 4 on real data.
- Could NOT run a Gradle build in the sandbox (wrapper needs cygpath/cmd.exe; only JDK is v25). Code reviewed manually and is sound; user should build in Android Studio.
- Rule/_pgmL_estimate.md updated with "embedded" status note.

## Proper standard-curve interpolation ("插补") + all samples embedded
- User: consider ALL sample data and do proper interpolation (not crude level→ladder medians).
- Discovered 测试用例.pptx and 样本.pptx carry IDENTICAL 9-level concentration ladders. 测试用例 = 9 measured test plates with known pg/mL (embedded verbatim as ground truth). 样本 = calibration reference.
- Built empirical (concentration, tealness) calibration from all 8 样本 photos + 3 clipboard photos using projection-based well detection + 90th-pct tealness inside liquid. High-conc standard photos (image5-8) give clean, monotonic tealness per factor column.
- New `model/StandardCurve.kt`: per-factor calibration knots + `concentrationFor(signal, factor)` piecewise-linear interpolation in signal space (the proper ELISA 插补). maxConcentration caps = 500/1000/500.
- Rewrote `data/BuiltInData.kt` to embed ALL 12 plates: 9 测试用例 (real conc, spread 2026-07-28…08-05) + 3 clipboard photos (conc interpolated via StandardCurve from measured signals: P1 0/0/0, P2 15.0/0.0/7.1, P3 50.0/75.0/130.8).
- `JointSenseViewModel.analyze()`: replaced placeholder `PredictionModel.predict` with `StandardCurve.concentrationFor(bMean-rMean, factor)` so live captures also interpolate via the calibrated curve. (PredictionModel.kt left as documented placeholder.)
- Updated Rule/_pgmL_estimate.md with the interpolation method + full 12-plate results.
- NOTE: sandbox bulk-delete guard blocked removing temp `_std` dir (~51 extracted images) and `~$测试用例_炎症因子标注.pptx` lock file this turn — both are harmless temp artifacts, delete later.

## Guided calibration UI (user asked "能添加一些校准吗？" → chose 引导式校准界面)
- Added a guided in-app calibration flow so the user can re-fit the ELISA standard curve from a photo of the kit's standard ladder plate (instead of relying on hardcoded factory knots).
- New files:
  - `model/Calibration.kt`: `CalibrationKnot`, `Calibration`, and `FACTORY_LADDER` (per-factor default 9-pt ladder used to pre-fill the well-concentration table).
  - `model/CalibrationDetector.kt`: `detectGridSignals()` subdivides the cropped plate into a regular 3×3 grid and measures tealness (B−R) at the centered liquid sub-region of each cell (robust; avoids fragile circle/peak detection).
  - `data/CalibrationManager.kt`: object that persists user calibration to a new SharedPreferences ("joint_sense_calibration") and merges per-factor; on `init()` loads + applies it to StandardCurve. `saveFactor()` merges one factor; `clear()` reverts to factory.
  - `ui/screens/CalibrationFlowScreen.kt`: 5-step flow (SELECT → CROP → ASSIGN → REVIEW → DONE). REVIEW background-corrects by subtracting the blank (conc 0) well signal, then saves net (conc, signal) knots.
- Refactored `model/StandardCurve.kt`: knots now overridable via `setKnots(Calibration)` / `resetKnots()`; public API (`concentrationFor`, `signalForConcentration`) unchanged. `concentrationFor` now uses the user curve when active → live `analyze()` automatically benefits.
- Wiring: `FlowScreen.CALIBRATION` added to enum; `JointSenseViewModel.init` calls `CalibrationManager.init(application)` and exposes `hasUserCalibration` / `calibrationFactorCount`; Profile tab gained a "Calibrate Standard Curve" entry; MainActivity routes the flow.
- Build NOT verified (Gradle sandbox-blocked). Code reviewed manually; user should build in Android Studio.
- FIX 2026-08-07 (compile error): ProfileScreen.kt:166 called `viewModel.hasUserCalibration()` as a function, but it is a `val` property on JointSenseViewModel (delegates to CalibrationManager.hasUserCalibration()). Changed to property access `viewModel.hasUserCalibration`. (calibrationFactorCount was already correct, also a property.)
- DOC 2026-08-07: wrote `项目结构需求梳理.md` at project root — full structure/requirements/decision-log retrospective for JointSense_v2 (tech stack, file map, ELISA tealness→pg/mL model, StandardCurve interpolation, OaIndex 0~4 grades/caps/weights, guided calibration, built-in 12-plate data, persistence, open risks incl. Gradle-sandbox build gap + photo-color-proxy vs OD450 4PL).
